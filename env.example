# DeltaGlider Commander Environment Variables
# Copy this file to .env and fill in your values

# IMPORTANT: S3/AWS credentials are NO LONGER configured via environment variables.
# After starting the application, navigate to http://localhost:8000/settings
# to configure your S3 credentials through the web UI. Your credentials will be
# stored in your browser's session storage and passed securely to the backend.

# Security (Required)
# Generate a strong secret: openssl rand -hex 32
DGCOMM_HMAC_SECRET=your-hmac-secret-here

# Optional Configuration
# DGCOMM_CACHE_DIR=/tmp/dgcommander-cache
# DGCOMM_OBJECT_RATE_LIMIT=10          # Max requests per window (default: 10)
# DGCOMM_OBJECT_RATE_WINDOW=1.0       # Window in seconds (default: 1.0 = 10 req/sec)
# DGCOMM_DOWNLOAD_TTL=300             # Download token lifetime in seconds (default: 5 minutes)
